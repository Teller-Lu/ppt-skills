# components/ — 跨风格组件库

> **阶段 4 填充**。当前为占位。

每个组件 = 一个**蒸馏过的、引擎安全的、带内容契约**的整页模板(不是 `pptx_to_svg` 原始转换 SVG)。

- 索引:`catalog.json`(每条元数据:`content_type` / `style_family` / `slots` / `color_roles` / `content_contract` / `technique_map`)。
- 本体:`<component-id>.svg`(+ 必要的烘焙资产 `<id>.assets/`)。
- 规格与判型/净化流程:见 `../../../docs/素材模型与组件库规格.md`。
- 蒸馏/回流/烘焙方法论:见 `../../../docs/保真与内容适配方法论.md`。
