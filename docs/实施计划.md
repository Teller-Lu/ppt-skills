# 实施计划 · ppt-skills(一期:A+B+C)

> 一期下注已锁定:**A 模板即"质量地板" + B 组件化复用 + C 美学质量门**。
> 三者协同:A 提供已验证的设计骨架,B 提供可组装的预制件,C 把"接近成品"变得可度量。
> 背景见 [优化方向与改进设计.md](优化方向与改进设计.md)、底图见 [ppt-master解析.md](ppt-master解析.md)。

## 进度(2026-06-13)

- 阶段 0 ✓ 立项+解析+定位;一期下注 **A+B+C 已锁定**。
- 阶段 1 ✓ 引擎**双向验证**(`pptx_to_svg` 零依赖解析 + `svg_to_pptx` 导出);依赖已装。
- 阶段 2 ✓ 自包含骨架:引擎裁剪进 `skills/ppt-skills/`(裁掉生图/配音)+ **SKILL.md v1**(9 纪律 + 8 步)+ `.claude-plugin/` + `requirements.txt` + `.gitignore`。
- 阶段 3 ✓ 第一套风格地板 `styles/academic-defense/`(brand + gate.calibration + page-inventory);提炼前净化 DLP 图章。
- **学习源修正 ✓(关键转向)**:发现"组件朴素"病根 = 一直从**有损的** `pptx_to_svg` SVG 学(丢图标 / 压渐变文字 / 圆变方)。改用 **PowerPoint 渲染真图 PNG**(= 用户所见)作学习源(`_experiments/academic_defense_25/png-truth/`),据真图重写 [技法库.md](技法库.md) v2(母题目录 + 真图标 + 幽灵描边)。
- 阶段 4 ✓ **11 个蒸馏组件**(cover〔真照片+缎带+footer图标〕/ toc / section〔烘焙数字〕/ seg-3 / seg-4 / comparison〔双圆对比〕/ hub〔六边形卫星〕/ timeline〔波浪锯齿〕/ table〔渐变表头+星级〕/ references / closing),全用真图标 `<use data-icon>` + 结构母题,入 `catalog.json` v0.2。
- 阶段 5 ✓ `scripts/aesthetic_checker.py`(stdlib)—— 缺陷检查 + **非朴素 richness 门**(太朴素 → `TOO_PLAIN`),阈值按风格校准;规格见 [美学质量门规格.md](美学质量门规格.md) §二·三。
- **全 deck 导出核验 ✓**:11 组件 → 嵌 16 真图标 → `svg_to_pptx` **11/11 原生 0 跳过** → PowerPoint 重渲染确认图标/母题/渐变/投影/星级全存活到可编辑 pptx(`_experiments/deck_full/`)。
- 阶段 6 ⏸ **暂停**(用户 2026-06-13:已见进步,需更多模板磨练后再做端到端)。
- 项目清理 ✓:`_experiments` 75M→26M,可复用脚本归入 `_experiments/_toolkit/`。
- **第二套 `business-blue` ✓(2026-06-14/15)**:128 页"工作型2.0"汇总册 → floor(brand+校准)+ **批A 4 段型**(`bz-seg2/3/4/multi`)+ **批B 3 件**(`bz-table`/`bz-data`/`bz-text`)+ **批C 6 件**(`bz-cover` 封面 / `bz-toc` 目录 / `bz-section` 章节 / `bz-team` 团队 / `bz-showcase` 图文 / `bz-closing` 结尾)= **13 件,`catalog.json` v1.0,首轮全页型覆盖(封面→结尾可成整套)**;全部真渲染 + 过门。新工具 `contact_sheet`/`floor_probe`/`preview_svgs` 入 `_toolkit`。关键认知:signature = **中心母题 + 多点放射**(纯单段、纯文字墙页均不存在);策略=**广度优先**每类先取 1 代表变体(用户 2026-06-14 定);新技法:**图片占位槽**(image slot + tabler 占位图标,运行时填用户图)、**出血天际线**(矩形组,id 含 `decor` 豁免门)、**大编号/水印**(渐变文字会被引擎压纯色 → 用实色或烘焙)。

## 阶段路线

### 阶段 0 · 立项 + 解析 + 重新定位 ✓(已完成)
建目录、落规划文档(README + 讨论纪要 + 设计方案 + ppt-master解析 + 优化方向 + 素材模型与组件库规格 + 美学质量门规格 + 运行时使用流程)。目标从"个性化"升级为"更好用、更接近成品"。

### 阶段 1 · 引擎验证(不需素材;**需你允许 `pip install`**)
1. 装裁剪依赖:`python-pptx`、`svglib`、`reportlab`、`flask`、`Pillow`、`numpy`。
2. 用 ppt-master 跑通一个**最小例子**(2–3 页测试 PPT → 导出 pptx),确认 **SVG→pptx 命脉在你机器(Python 3.10.1)上通**。
> 通过 = 引擎可用,放心复制。

### 阶段 2 · 自包含骨架(不需素材)
把裁剪后的 `scripts/` + `references/` 复制进 `skills/ppt-skills/`;写 **SKILL.md v1**(我们的编排:**默认走风格库地板**、关生图、关配音);建空的 `styles/` 与 `components/`。

### 阶段 3 · A:第一套风格 = "质量地板"(**首跑 ✓ 2026-06-12**)
> 素材分**三型**(单风格型 / 单内容汇总型 / 汇总型),先**判型**;第一套(学术答辩)是单风格型,走成套提炼。详见 [素材模型与组件库规格.md](素材模型与组件库规格.md)。
> **0 · 提炼前净化**:剔除公司 DLP 保密图章 + 装饰水印(全局规则,§五·0)。✓ 已清 24 页图章。
1. `pptx_to_svg` 零依赖把模板拆成 SVG → 看清骨架与配色;**边解析边把每页归类成组件、生长组件分类、量出美学门阈值**(写入 `gate.calibration.json`)。✓ 已产出 `styles/academic-defense/`。
2. 提炼成 `styles/<风格名>/`:
   - `brand.md` — 配色 / 字体 / **间距栅格** / **字号体系** / 调性(审美 DNA)
   - `layouts/` — 招牌页型骨架(封面 / 目录 / 章节 / 正文 / 图表框 / 结尾)
3. 把这套接进 SKILL.md 作**默认设计骨架**(不是 opt-in)。
> ⚠️ 防"地板变天花板":模板给**结构与基线**,内容仍由 AI 按页填充微调,不锁死成一个模子。

### 阶段 4 · B:组件化复用 ✓(完成 2026-06-13)
1. 从拆出的 SVG 抽 **vetted 区块库** `components/`(封面 / 章节 / KPI 数字 / 双栏对比 / 时间线 / 图表框 / 引用页…),每块带元数据(用途 / 尺寸 / 锚点)。
2. 改 Executor 流程:带完整上下文"**挑组件 → 组装 → 逐页微调**"。
> 🔴 守红线:这是"主代理在上下文里组装预制件",**不是**被禁的"生成器脚本批量产 SVG"。

### 阶段 5 · C:美学质量门 ✓(完成 2026-06-13)
1. 写 `aesthetic_checker`(接在 ppt-master 技术质量门 `svg_quality_checker` 之后,跑在 `svg_output/`):
   - 文字溢出框 / 元素碰撞重叠 / 对比度不足 / 越过安全边距 / 不贴对齐栅格 / 字号低于下限
   - **阈值按你的模板系统校准**(栅格、字号体系来自阶段 3 的 brand.md)
2. 严重性沿用 ppt-master:`error` 阻塞 → 定向重生成该页;**无 auto-fix**(保留设计意图)。
> 检查项 / 阈值 / 校准的完整 v1 规格:见 [美学质量门规格.md](美学质量门规格.md)。

### 阶段 6 · 端到端验证 ⏸(暂停:待更多模板磨练后再做)
给一段测试内容 → **默认走第一套风格** → 出 pptx;检查:① 像不像你的审美 ② 是否更"接近成品"(少手工)③ 美学质量门通过。

### 阶段 7 · push GitHub
建新 repo,`git push`(work-pc 走 `push_via_proxy.sh`);补 README / LICENSE(署名 ppt-master) / requirements。

### 阶段 8 · 扩充 + 二期
再加商务 / 技术 / 学术其余风格;二期评估 D(图表真实绘图)/ E(express 快捷路径)/ F(对话式逐页修改)。

## 当前进行 / 下一步

- ✅ 第一套(单风格型 · 学术答辩):floor + 11 华丽组件 + 美学门 + 导出核验,**样板**立住。
- ✅ **第二套(汇总型 · 工作型2.0 · 128页 = `business-blue`)批A+B+C 全学成**:floor + 段型(2/3/4/多)+ 表格/数据看板/整段说明 + 封面/目录/章节/团队/图文/结尾 = **13 件,catalog v1.0,首轮全页型覆盖(封面→结尾可成整套)**。管线已验证**跨风格泛化**(新调色板/字体/校准 + 真图标 + 新母题 + 图片占位槽 + 出血天际线 + 大编号/水印)。
- ⏭ **下一步(可选,待用户定)**:① 批C 补合作伙伴 logo 墙(p093–096);② 各类型**多风格变体**(三段 p031/p036、四段四卡、数据饼/环/折线、单人/双人、手机样机);③ **阶段6 真实内容端到端验收**;④ **阶段7 push GitHub**(ppt-skills 尚非 git repo;work-pc 须用 push_via_proxy.sh)。
- 之后回到**阶段 6**(选方向做真实内容端到端)。

> 学新模板的标准流程见 [文件系统说明.md](文件系统说明.md) 末"用 `_toolkit/`"一节。

## 你不用做的
装 Python(已 3.10.1)、写任何代码、配生图 key。
